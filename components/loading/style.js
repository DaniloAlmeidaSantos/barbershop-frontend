import * as React from 'react';
import styled from 'styled-components/native';
import Colors from '../../constants/Theme';

export const Loading_Container = styled.View`
  flex: 1;
  justify-content: center
  align-items: center;
  align-self: center;

  margin-top: -200px;
`;

export const Loading_Text = styled.Text`
  margin-top: 10px;

  font-family: 'Segoe';
  font-size: 20px;
`
